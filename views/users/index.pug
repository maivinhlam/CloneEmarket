doctype html
html(lang='en')
  head
    meta(charset='UTF-8')
    meta(name='viewport', content='width=device-width, initial-scale=1.0')
    title Document
  body   
    h1 Users
    a(href="/users/create") Create User
    form(action='users/search', mothod="GET")
        input(type="text", name="q" )
        button Search
    ul  
      each user in users
        li
            span= user.name
            span &nbsp;
            a(href="/users/" + user.id) View

        
